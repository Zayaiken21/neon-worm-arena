services:
  - type: web
    name: slither-live-compact
    env: node
    plan: free
    buildCommand: npm install --no-audit --no-fund
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 20.20.2
