USE THESE EXACT RENDER SETTINGS

Environment: Node
Build Command: npm install --no-audit --no-fund --loglevel=error
Start Command: npm start
Root Directory: leave blank

IMPORTANT FIX:
Do NOT upload package-lock.json. The old lock file had internal package URLs that Render cannot access.
This fixed folder intentionally does not include package-lock.json.

After updating GitHub:
Manual Deploy > Clear build cache & deploy
