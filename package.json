{
  "name": "slither-pro-live-v2",
  "version": "2.0.0",
  "private": true,
  "description": "Compact HTML multiplayer slither arena with zero dependencies for Render.",
  "engines": { "node": ">=20 <21" },
  "scripts": {
    "build": "node -e \"console.log('No build needed: compact HTML game ready')\"",
    "start": "node server.js"
  },
  "dependencies": {}
}
