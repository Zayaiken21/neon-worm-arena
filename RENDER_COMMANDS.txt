RENDER SETTINGS

Service Type: Web Service
Environment: Node
Root Directory: leave blank if these files are at the repo root
Build Command: npm install
Start Command: npm start

If Render still shows rpm install, go to Settings and manually replace it. Then use Manual Deploy -> Clear build cache & deploy.
