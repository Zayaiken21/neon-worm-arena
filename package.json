{
  "name": "slither-pro-ready",
  "version": "1.0.0",
  "private": true,
  "description": "Compact HTML multiplayer slither arena for Render.",
  "engines": { "node": ">=20 <21" },
  "scripts": {
    "build": "node -e \"console.log('No build step needed - compact HTML server ready')\"",
    "start": "node server.js"
  }
}
